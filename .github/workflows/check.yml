name: CheckInfo

on:
  workflow_dispatch:
    inputs:
      CheckInfo:
        description: "Check font info"
        required: true
        type: boolean

jobs:
  CheckInfo:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download 
      run: |
        mkdir check && cd check
        curl -L -o file.zip "https://github.com/5hes/MyToolBox/releases/download/v0.3.0/Arm.Compiler.for.Embedded.FuSa.6.22.1.Toolchain.and.Qualification.Kit.AArch64.Linux.zip"
        
    - name: Extra
      run: |
        cd check
        unzip file.zip
        rm file.zip
        tar -xf *.tar.gz

    - name: Get info
      run: |
        cd check
        ls -ltrah
        ls -ltra
        cd *
        ls -ltrah
        ls -ltra

        echo -e "\n\n\n\n\n11111"
        tree -L 2
        
        echo -e "\n\n\n\n\n22222"

    - name: Tree
      run: tree